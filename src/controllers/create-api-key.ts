import { Request, Response } from "express";

const createApiKey = async (req: Request, res: Response) => {
  const { owner_id, fly_id } = req.body;

  try {
  } catch (error) {}
};

export { createApiKey };
